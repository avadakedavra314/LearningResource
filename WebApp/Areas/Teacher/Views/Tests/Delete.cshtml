@model WebApp.Models.Test
@{
    ViewBag.Title = "Удаление теста";
}

<ol class="breadcrumb">
    <li><a asp-area="" asp-action="Index" asp-controller="Home">Главная</a></li>
    <li><a asp-area="Teacher" asp-action="Index" asp-controller="Tests">Тесты</a></li>
    <li class="active">@ViewBag.Title</li>
</ol>

<div class="container-fluid">
    <div class="panel panel-primary">
        <div class="panel-heading">@ViewBag.Title</div>
        <table class="table">
            <tbody>
                <tr>
                    <th scope="row">Id</th>
                    <td>@Model.Id</td>
                </tr>
                <tr>
                    <th scope="row">Название</th>
                    <td>@Model.Name</td>
                </tr>
                <tr>
                    <th scope="row">Курс</th>
                    <td>
                        @Model.Course.Name
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
    <form asp-action="Delete" method="post">
        <div class="form-group">
            <input type="submit" class="btn btn-primary btn-block" value="Удалить" />
        </div>
        <a asp-action="Index" asp-area="Teacher" asp-controller="Tests" class="btn btn-default btn-block" role="button">Назад</a>
    </form>
</div>